package com.example.pokedexcompose.data.repository

import org.junit.Assert.*

class HomeRepositoryImplTest {

    /**
    HomeRepositoryImpl contem um unico metodo (getPokemonList(): Flow<PagingData<PokemonAndDetail>>),
    porém, PagingData é apenas um fluxo sem estado de eventos de carga incremental, portanto, não dá
    para analizar o estado do conteudo.
     */

}